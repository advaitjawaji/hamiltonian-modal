defaults:
  - env: g1_walking
  - world_model: default
  - policy: mlp
  - mpc: ilqr
  - training: default

seed: 42
output_dir: outputs/${now:%Y-%m-%d_%H-%M-%S}
wandb:
  enabled: false
  project: hamiltonian-modal
  entity: null
