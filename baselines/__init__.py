"""Baseline adapters for hamiltonian-modal comparative evaluation.

Each sub-package provides a thin adapter that maps the
:class:`~hamiltonian_modal.envs.g1_base.G1BaseEnv` interface to the
external baseline's API without requiring the external library to be installed.
"""
